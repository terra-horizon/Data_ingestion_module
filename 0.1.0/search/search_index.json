{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Data Ingestion Service","text":"<p>The Data Ingestion Service is currently an alpha version of the TERRA Data Ingestion Module.</p> <p>This alpha focuses on validating external-provider access, Copernicus/CDSE calls, request normalization, and response wrapping. It is intentionally stateless while the wider TERRA orchestration, storage, and persistence responsibilities are still being finalized.</p> <p>The service is a stateless HTTP module used by TERRA services to replace direct calls to external data providers.</p> <p>Other modules send provider requests to this service. The service validates and normalizes the request, calls the external provider through a source adapter, transforms the provider response through a wrapper, and returns the result to the caller.</p> <p>It does not own workflow state.</p>"},{"location":"#alpha-scope","title":"Alpha Scope","text":"<p>This alpha implementation provides the connector and wrapper foundation of the full Data Ingestion Module. It does not yet implement the complete production architecture described for TERRA Big Data integration.</p>"},{"location":"#what-it-does","title":"What It Does","text":"<ul> <li>Receives ingestion requests over HTTP.</li> <li>Calls external providers through isolated adapters.</li> <li>Wraps provider responses into standard or compatibility formats.</li> <li>Returns the transformed response immediately.</li> <li>Keeps Copernicus Data Space Ecosystem as the first supported provider.</li> </ul>"},{"location":"#current-provider-support","title":"Current Provider Support","text":"<p>The current implementation registers one source adapter:</p> Source Supported modes Purpose <code>copernicus</code> <code>stac</code>, <code>sentinel-hub-catalog</code>, <code>sentinel-hub-process</code> Catalogue search and stateless Sentinel Hub Process API pass-through <p>The Sentinel Hub Process mode returns file bytes as base64 in the HTTP response. The caller decides where to store the file.</p>"},{"location":"#request-path","title":"Request Path","text":"<p>The request path is:</p> <ol> <li>A calling TERRA module sends <code>POST /api/ingestion/run</code>.</li> <li>The request normalizer validates and standardizes the payload.</li> <li>The source registry selects the configured provider adapter.</li> <li>The Copernicus adapter calls the external CDSE API.</li> <li>The wrapper registry selects the requested response profile.</li> <li>The response wrapper transforms the provider response.</li> <li>The service returns a stateless HTTP response to the caller.</li> </ol>"},{"location":"#main-endpoint","title":"Main Endpoint","text":"<p>Use <code>POST /api/ingestion/run</code> for all current ingestion calls.</p> <pre><code>curl -X POST http://localhost:3000/api/ingestion/run \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"source\": \"copernicus\",\n    \"mode\": \"sentinel-hub-catalog\",\n    \"collection\": \"sentinel-2-l2a\",\n    \"datasetType\": \"catalogue\",\n    \"format\": \"json\",\n    \"responseProfile\": \"scene-search-compatibility\",\n    \"requestParams\": {\n      \"bbox\": [22.1, 39.4, 22.8, 40.1],\n      \"dateFrom\": \"2025-01-01\",\n      \"dateTo\": \"2025-01-31\",\n      \"maxImages\": 5,\n      \"maxCloudPct\": 20,\n      \"cloudCoverageMax\": 20\n    },\n    \"download\": false\n  }'\n</code></pre> <p>See the API Reference for all endpoints and response profiles.</p>"},{"location":"api-reference/","title":"API Reference","text":"<p>This API reference describes the alpha stateless API. It does not expose job, storage, database, or workflow-management endpoints yet.</p> <p>Base URL for local development:</p> <pre><code>http://localhost:3000\n</code></pre>"},{"location":"api-reference/#get-apihealth","title":"GET /api/health","text":"<p>Returns basic service status.</p> <pre><code>curl http://localhost:3000/api/health\n</code></pre> <p>Response:</p> <pre><code>{\n  \"success\": true,\n  \"service\": \"data-ingestion-service\",\n  \"status\": \"ok\",\n  \"mode\": \"stateless\"\n}\n</code></pre>"},{"location":"api-reference/#get-apisources","title":"GET /api/sources","text":"<p>Lists registered source adapters.</p> <pre><code>curl http://localhost:3000/api/sources\n</code></pre> <p>Response:</p> <pre><code>{\n  \"success\": true,\n  \"sources\": [\n    {\n      \"name\": \"copernicus\",\n      \"adapter\": \"CopernicusAdapter\"\n    }\n  ]\n}\n</code></pre>"},{"location":"api-reference/#get-apisourcessourcehealth","title":"GET /api/sources/:source/health","text":"<p>Calls the selected source adapter health check.</p> <pre><code>curl http://localhost:3000/api/sources/copernicus/health\n</code></pre> <p>The exact health payload depends on the adapter.</p>"},{"location":"api-reference/#post-apiingestionrun","title":"POST /api/ingestion/run","text":"<p>Executes one stateless ingestion call.</p> <p>In the alpha version, this endpoint returns transformed data directly. It does not return MongoDB entry IDs, MinIO paths, job IDs, or persistent storage locations.</p> <p>The response shape is:</p> <pre><code>{\n  \"success\": true,\n  \"source\": \"copernicus\",\n  \"mode\": \"sentinel-hub-catalog\",\n  \"collection\": \"sentinel-2-l2a\",\n  \"responseProfile\": \"scene-search-compatibility\",\n  \"data\": [],\n  \"metadata\": {}\n}\n</code></pre>"},{"location":"api-reference/#standard-stac-catalogue","title":"Standard STAC Catalogue","text":"<p>Use <code>responseProfile: \"standard\"</code> for the platform catalogue object.</p> <pre><code>curl -X POST http://localhost:3000/api/ingestion/run \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"source\": \"copernicus\",\n    \"mode\": \"stac\",\n    \"collection\": \"sentinel-2-l2a\",\n    \"datasetType\": \"catalogue\",\n    \"format\": \"json\",\n    \"responseProfile\": \"standard\",\n    \"requestParams\": {\n      \"bbox\": [22.1, 39.4, 22.8, 40.1],\n      \"dateFrom\": \"2025-01-01\",\n      \"dateTo\": \"2025-01-31\",\n      \"limit\": 5,\n      \"cloudCoverageMax\": 20\n    },\n    \"download\": false\n  }'\n</code></pre>"},{"location":"api-reference/#scene-search-compatibility","title":"Scene Search Compatibility","text":"<p>Use this profile to replace the existing Python <code>_search_scenes_cdse(...)</code> function.</p> <pre><code>curl -X POST http://localhost:3000/api/ingestion/run \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"source\": \"copernicus\",\n    \"mode\": \"sentinel-hub-catalog\",\n    \"collection\": \"sentinel-2-l2a\",\n    \"datasetType\": \"catalogue\",\n    \"format\": \"json\",\n    \"responseProfile\": \"scene-search-compatibility\",\n    \"requestParams\": {\n      \"bbox\": [22.1, 39.4, 22.8, 40.1],\n      \"dateFrom\": \"2025-01-01\",\n      \"dateTo\": \"2025-01-31\",\n      \"maxImages\": 5,\n      \"maxCloudPct\": 20,\n      \"cloudCoverageMax\": 20\n    },\n    \"download\": false\n  }'\n</code></pre> <p>The <code>data</code> field is a list matching the old function contract:</p> <pre><code>[\n  {\n    \"scene_id\": \"S2C_MSIL2A_...\",\n    \"datetime\": \"2025-01-27T09:29:53.031Z\",\n    \"cloud_pct\": 11.75,\n    \"collection\": \"sentinel-2-l2a\",\n    \"bbox\": [22.1, 39.4, 22.8, 40.1],\n    \"properties\": {\n      \"platform\": \"sentinel-2c\",\n      \"constellation\": \"sentinel-2\"\n    }\n  }\n]\n</code></pre>"},{"location":"api-reference/#scene-download-compatibility","title":"Scene Download Compatibility","text":"<p>Use this profile to replace the existing Python <code>_download_scene_cdse(...)</code> call.</p> <p>The Node service remains stateless. It calls CDSE Process API and returns TIFF bytes as base64. The caller writes the file to its own target path.</p> <pre><code>curl -X POST http://localhost:3000/api/ingestion/run \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"source\": \"copernicus\",\n    \"mode\": \"sentinel-hub-process\",\n    \"collection\": \"sentinel-2-l2a\",\n    \"datasetType\": \"image\",\n    \"format\": \"tiff\",\n    \"responseProfile\": \"scene-download-compatibility\",\n    \"requestParams\": {\n      \"bbox\": [22.1, 39.4, 22.8, 40.1],\n      \"scene\": {\n        \"scene_id\": \"S2C_MSIL2A_...\",\n        \"datetime\": \"2025-01-27T09:29:53.031Z\"\n      }\n    },\n    \"download\": true\n  }'\n</code></pre> <p>Response <code>data</code> contains:</p> <pre><code>{\n  \"scene_id\": \"S2C_MSIL2A_...\",\n  \"datetime\": \"2025-01-27T09:29:53.031Z\",\n  \"contentType\": \"image/tiff\",\n  \"format\": \"tiff\",\n  \"dataBase64\": \"...\",\n  \"sizeBytes\": 123456,\n  \"width\": 512,\n  \"height\": 512,\n  \"bbox\": [22.1, 39.4, 22.8, 40.1]\n}\n</code></pre>"},{"location":"api-reference/#error-response","title":"Error Response","text":"<p>All handled errors use:</p> <pre><code>{\n  \"success\": false,\n  \"message\": \"error\",\n  \"code\": \"ERROR_CODE\"\n}\n</code></pre> <p>Common error codes:</p> Code Meaning <code>VALIDATION_ERROR</code> Request payload failed validation. <code>INVALID_REQUEST</code> The requested provider operation is not valid. <code>UNKNOWN_SOURCE</code> No adapter is registered for the requested source. <code>UNSUPPORTED_MODE</code> The adapter does not support the requested mode. <code>DOWNLOAD_NOT_SUPPORTED</code> Download was requested for a mode that does not support it. <code>COPERNICUS_AUTH_MISSING</code> CDSE credentials or token are missing. <code>COPERNICUS_AUTH_ERROR</code> Token acquisition failed. <code>EXTERNAL_API_ERROR</code> External provider returned an error response. <code>EXTERNAL_API_TIMEOUT</code> External request timed out. <code>UNSUPPORTED_WRAPPER</code> No wrapper supports the requested response profile. <code>ROUTE_NOT_FOUND</code> No matching route exists."},{"location":"architecture/","title":"Architecture","text":"<p>This is the alpha version of the Data Ingestion Module architecture.</p> <p>The service is intentionally lightweight in its current form and is designed primarily to validate external integrations, data wrapper implementations, and provider communication workflows. It is an HTTP boundary around external-provider calls and response transformation, not the complete persistence-owning ingestion subsystem.</p>"},{"location":"architecture/#runtime-components","title":"Runtime Components","text":"<p>The runtime is split into four small layers:</p> <ul> <li>Express layer: <code>app.js</code>, routes, controllers, and middleware expose the HTTP API and handle request/response concerns.</li> <li>Core layer: <code>ingestion.service.js</code>, <code>request-normalizer.js</code>, <code>source.registry.js</code>, and <code>wrapper.registry.js</code> coordinate validation, provider selection, and response shaping.</li> <li>Provider layer: source adapters, currently including <code>copernicus.adapter.js</code>, isolate external-provider communication.</li> <li>Output layer: wrappers such as <code>standard-catalogue.wrapper.js</code>, <code>copernicus-compatibility.wrapper.js</code>, and <code>scene-search-compatibility.wrapper.js</code> transform provider responses into caller-facing contracts.</li> </ul> <p>Requests enter through Express routes, pass through controllers into the ingestion service, call the selected source adapter, and return through the selected wrapper before the HTTP response is sent.</p>"},{"location":"architecture/#stateless-boundary","title":"Stateless Boundary","text":"<p>The alpha module uses request-local variables while handling a request, but it does not persist anything after the response is returned.</p> <p>The service has no:</p> <ul> <li>job status map</li> <li>queue consumer</li> <li>database client</li> <li>dataset repository</li> <li>storage service</li> <li>ingestion history table</li> </ul> <p>In the alpha version, these capabilities are intentionally out of scope and are not implemented within the service.</p>"},{"location":"architecture/#core-flow","title":"Core Flow","text":"<p>The core request flow is:</p> <ol> <li>The TERRA orchestrator or caller sends <code>POST /api/ingestion/run</code> to the Express API.</li> <li>The API passes the payload to <code>runIngestion(payload)</code>.</li> <li>The ingestion service normalizes and validates the request.</li> <li>The selected source adapter fetches data from Copernicus/CDSE.</li> <li>The adapter returns raw provider data, external request metadata, and provider metadata.</li> <li>The selected wrapper transforms the raw data into the requested response profile.</li> <li>The API returns the transformed result as a stateless JSON response.</li> </ol>"},{"location":"architecture/#extension-points","title":"Extension Points","text":"<p>Add a new provider by creating a source adapter and registering it in <code>src/sources/source.registry.js</code>.</p> <p>Add a new response contract by creating a wrapper and registering it in <code>src/wrappers/wrapper.registry.js</code>.</p> <p>The ingestion service should not need provider-specific controller logic.</p>"},{"location":"configuration/","title":"Configuration","text":"<p>This configuration applies to the alpha stateless implementation. Variables for MongoDB, MinIO, queues, IoT brokers, or workflow state are intentionally absent because those integrations are not implemented in this version.</p> <p>Configuration is loaded from environment variables through <code>src/config/env.js</code>.</p> <p>Copy <code>.env.example</code> to <code>.env</code> for local development.</p> <pre><code>cp .env.example .env\n</code></pre>"},{"location":"configuration/#environment-variables","title":"Environment Variables","text":"Variable Default Required Description <code>PORT</code> <code>3000</code> No HTTP server port. <code>NODE_ENV</code> <code>development</code> No Runtime environment. Controls logging format. <code>REQUEST_TIMEOUT_MS</code> <code>30000</code> No Axios request timeout in milliseconds. <code>MAX_CATALOGUE_LIMIT</code> <code>100</code> No Maximum accepted catalogue limit from caller payloads. <code>COPERNICUS_API_MODE</code> <code>stac</code> No Default Copernicus mode when payload does not provide <code>mode</code>. <code>COPERNICUS_STAC_BASE_URL</code> <code>https://stac.dataspace.copernicus.eu/v1</code> No STAC base URL used by <code>mode: \"stac\"</code>. <code>COPERNICUS_SH_CATALOG_URL</code> <code>https://sh.dataspace.copernicus.eu/api/v1/catalog/1.0.0/search</code> Yes for <code>sentinel-hub-catalog</code> Sentinel Hub Catalog endpoint. <code>COPERNICUS_SH_PROCESS_URL</code> <code>https://sh.dataspace.copernicus.eu/api/v1/process</code> Yes for <code>sentinel-hub-process</code> Sentinel Hub Process endpoint. <code>COPERNICUS_ODATA_BASE_URL</code> <code>https://catalogue.dataspace.copernicus.eu/odata/v1</code> No Reserved for future OData catalogue support. <code>COPERNICUS_TOKEN_URL</code> CDSE OpenID Connect token endpoint Required when using client credentials Token endpoint used when <code>COPERNICUS_ACCESS_TOKEN</code> is not set. <code>COPERNICUS_USERNAME</code> empty No Reserved for future username/password flows. <code>COPERNICUS_PASSWORD</code> empty No Reserved for future username/password flows. <code>COPERNICUS_ACCESS_TOKEN</code> empty Required unless client credentials are provided for authenticated Sentinel Hub modes Bearer token used directly for CDSE requests. <code>COPERNICUS_CLIENT_ID</code> empty Required when no access token is provided for authenticated Sentinel Hub modes OAuth client ID. <code>COPERNICUS_CLIENT_SECRET</code> empty Required when no access token is provided for authenticated Sentinel Hub modes OAuth client secret."},{"location":"configuration/#authentication-behavior","title":"Authentication Behavior","text":"<p>For <code>sentinel-hub-catalog</code> and <code>sentinel-hub-process</code>:</p> <ol> <li>If <code>COPERNICUS_ACCESS_TOKEN</code> is set, the adapter sends it as a Bearer token.</li> <li>If no access token is set, the adapter requests a token using <code>COPERNICUS_CLIENT_ID</code> and <code>COPERNICUS_CLIENT_SECRET</code>.</li> <li>Tokens are not persisted by the service.</li> </ol> <p>Secrets must never be committed to Git.</p>"},{"location":"configuration/#docker-configuration","title":"Docker Configuration","text":"<p>The Docker image reads the same environment variables as local Node execution.</p> <p>For local Compose usage, create <code>.env</code> at the repository root:</p> <pre><code>cp .env.example .env\n</code></pre> <p>Docker Compose loads this file automatically:</p> <pre><code>docker compose up --build\n</code></pre> <p>For direct <code>docker run</code>, pass the file explicitly:</p> <pre><code>docker run --rm --env-file .env -p 3000:3000 data-ingestion-module\n</code></pre> <p>Do not bake credentials into the image. Provide <code>COPERNICUS_ACCESS_TOKEN</code>, <code>COPERNICUS_CLIENT_ID</code>, and <code>COPERNICUS_CLIENT_SECRET</code> through environment variables or the deployment platform secret manager.</p>"},{"location":"deployment/","title":"Deployment","text":"<p>This deployment guide applies to the alpha stateless version of the Data Ingestion Module.</p> <p>The service runs as a standard Node.js HTTP process.</p> <p>It currently has no database, queue, or filesystem storage dependency because persistence and workflow state are outside the alpha scope.</p>"},{"location":"deployment/#local-development","title":"Local Development","text":"<p>Install dependencies:</p> <pre><code>npm install\n</code></pre> <p>Create a local environment file:</p> <pre><code>cp .env.example .env\n</code></pre> <p>Start the API:</p> <pre><code>npm run dev\n</code></pre> <p>The default server URL is:</p> <pre><code>http://localhost:3000\n</code></pre>"},{"location":"deployment/#health-check","title":"Health Check","text":"<p>Use:</p> <pre><code>curl http://localhost:3000/api/health\n</code></pre> <p>Expected response:</p> <pre><code>{\n  \"success\": true,\n  \"service\": \"data-ingestion-service\",\n  \"status\": \"ok\",\n  \"mode\": \"stateless\"\n}\n</code></pre>"},{"location":"deployment/#local-docker-run","title":"Local Docker Run","text":"<p>Build the image from the repository root:</p> <pre><code>docker build -t data-ingestion-module .\n</code></pre> <p>Run the container with environment variables from <code>.env</code>:</p> <pre><code>docker run --rm --name data-ingestion-module --env-file .env -p 3000:3000 data-ingestion-module\n</code></pre> <p>Check health:</p> <pre><code>curl http://localhost:3000/api/health\n</code></pre>"},{"location":"deployment/#docker-compose-run","title":"Docker Compose Run","text":"<p>Start the service:</p> <pre><code>docker compose up --build\n</code></pre> <p>Run in the background:</p> <pre><code>docker compose up --build -d\n</code></pre> <p>Stop the service:</p> <pre><code>docker compose down\n</code></pre> <p>The Compose file loads <code>.env</code>, maps <code>${PORT:-3000}</code>, and defines a health check against <code>/api/health</code>.</p>"},{"location":"deployment/#production-deployment-notes","title":"Production Deployment Notes","text":"<p>Required runtime inputs:</p> <ul> <li><code>PORT</code></li> <li><code>REQUEST_TIMEOUT_MS</code></li> <li>Copernicus endpoint URLs</li> <li><code>COPERNICUS_ACCESS_TOKEN</code> or <code>COPERNICUS_CLIENT_ID</code> plus <code>COPERNICUS_CLIENT_SECRET</code> for authenticated Sentinel Hub modes</li> </ul> <p>Operational notes:</p> <ul> <li>expose the configured <code>PORT</code></li> <li>use <code>/api/health</code> for health checks</li> <li>inspect logs with the container platform logs command, for example <code>docker logs data-ingestion-module</code></li> <li>scale horizontally by running more container instances behind a load balancer</li> <li>keep provider credentials in platform secrets or environment variables</li> <li>do not add database, queue, or storage containers unless the architecture changes</li> </ul>"},{"location":"faq/","title":"FAQ","text":""},{"location":"faq/#why-do-wrappers-exist","title":"Why do wrappers exist?","text":"<p>Wrappers define the response contract returned to calling modules.</p> <p>Use <code>standard</code> for platform-friendly catalogue results. Use compatibility profiles when another team already expects a specific shape from an older direct-provider function.</p>"},{"location":"faq/#can-the-service-support-another-provider","title":"Can the service support another provider?","text":"<p>Yes, the architecture is designed to scale. You can add a new provider by creating a dedicated source adapter and registering it within <code>source.registry.js</code>.</p>"},{"location":"faq/#can-the-service-support-another-response-shape","title":"Can the service support another response shape?","text":"<p>Yes, it is fully flexible. Supporting a new response shape simply requires implementing a new wrapper and registering it in <code>wrapper.registry.js</code>.</p>"},{"location":"faq/#how-can-the-service-be-scaled","title":"How can the service be scaled?","text":"<p>Run multiple container instances behind a load balancer. The service does not keep per-instance job state, so horizontal scaling is safe as long as provider credentials and external API rate limits are handled.</p>"},{"location":"faq/#how-are-provider-credentials-handled","title":"How are provider credentials handled?","text":"<p>The service reads credentials from environment variables. It does not hardcode secrets and does not persist tokens beyond normal process memory.</p>"},{"location":"license/","title":"License","text":"<p>This repository is licensed under the Apache License, Version 2.0.</p> <p>See the repository LICENSE file for the full license text.</p>"},{"location":"pipeline-test-scenarios/","title":"Pipeline Test Scenarios","text":"<p>These scenarios verify the alpha service as a stateless replacement for direct external calls.</p> <p>They do not validate MongoDB persistence, MinIO storage, orchestrator callbacks, or IoT ingestion because those features are outside the current alpha implementation.</p>"},{"location":"pipeline-test-scenarios/#prerequisites","title":"Prerequisites","text":"<p>Install Node dependencies:</p> <pre><code>npm install\n</code></pre> <p>Configure <code>.env</code> with either:</p> <pre><code>COPERNICUS_ACCESS_TOKEN=...\n</code></pre> <p>or:</p> <pre><code>COPERNICUS_CLIENT_ID=...\nCOPERNICUS_CLIENT_SECRET=...\n</code></pre> <p>Start the service:</p> <pre><code>npm run dev\n</code></pre>"},{"location":"pipeline-test-scenarios/#scenario-1-health-check","title":"Scenario 1: Health Check","text":"<pre><code>curl http://localhost:3000/api/health\n</code></pre> <p>Expected:</p> <pre><code>{\n  \"success\": true,\n  \"service\": \"data-ingestion-service\",\n  \"status\": \"ok\",\n  \"mode\": \"stateless\"\n}\n</code></pre>"},{"location":"pipeline-test-scenarios/#scenario-2-registered-sources","title":"Scenario 2: Registered Sources","text":"<pre><code>curl http://localhost:3000/api/sources\n</code></pre> <p>Expected source list includes <code>copernicus</code>.</p>"},{"location":"pipeline-test-scenarios/#scenario-3-direct-vs-ingestion-scene-search","title":"Scenario 3: Direct Vs Ingestion Scene Search","text":"<p>Run:</p> <pre><code>python tests_external/compare_scene_search.py\n</code></pre> <p>The script:</p> <ul> <li>calls CDSE directly</li> <li>calls the ingestion service</li> <li>prints both outputs</li> <li>compares scene IDs</li> <li>compares ordering</li> <li>compares full output equality</li> </ul> <p>Expected comparison:</p> <pre><code>{\n  \"only_direct\": [],\n  \"only_ingestion\": [],\n  \"same_order\": true,\n  \"same_full_output\": true\n}\n</code></pre>"},{"location":"pipeline-test-scenarios/#scenario-4-direct-vs-ingestion-scene-download","title":"Scenario 4: Direct Vs Ingestion Scene Download","text":"<p>Run:</p> <pre><code>python tests_external/compare_scene_download.py\n</code></pre> <p>The script:</p> <ul> <li>searches for a scene</li> <li>downloads the TIFF directly from CDSE</li> <li>downloads the TIFF through the ingestion service</li> <li>writes both files under <code>tests_external/output</code></li> <li>compares byte size and SHA-256</li> </ul> <p>Expected:</p> <ul> <li>matching file size</li> <li>matching SHA-256</li> </ul> <p>The Node service does not store either file.</p>"},{"location":"pipeline-test-scenarios/#scenario-5-validation-failure","title":"Scenario 5: Validation Failure","text":"<p>Send an invalid bounding box:</p> <pre><code>curl -X POST http://localhost:3000/api/ingestion/run \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"source\": \"copernicus\",\n    \"requestParams\": {\n      \"bbox\": [22.1, 39.4]\n    }\n  }'\n</code></pre> <p>Expected:</p> <pre><code>{\n  \"success\": false,\n  \"message\": \"bbox must contain exactly four numbers\",\n  \"code\": \"VALIDATION_ERROR\"\n}\n</code></pre>"},{"location":"pipeline-test-scenarios/#scenario-6-missing-credentials","title":"Scenario 6: Missing Credentials","text":"<p>Run a Sentinel Hub Catalog request without <code>COPERNICUS_ACCESS_TOKEN</code> or client credentials.</p> <p>Expected error code:</p> <pre><code>COPERNICUS_AUTH_MISSING\n</code></pre>"},{"location":"pipeline-test-scenarios/#scenario-7-timeout-behavior","title":"Scenario 7: Timeout Behavior","text":"<p>Set a very low timeout:</p> <pre><code>REQUEST_TIMEOUT_MS=1\n</code></pre> <p>Then run a provider request.</p> <p>Expected error code:</p> <pre><code>EXTERNAL_API_TIMEOUT\n</code></pre>"},{"location":"qa/","title":"Quality Assurance","text":"<p>QA for this alpha version focuses on HTTP availability, request normalization, Copernicus/CDSE connectivity, wrappers, and direct-vs-ingestion compatibility.</p>"},{"location":"qa/#what-to-verify-before-handoff","title":"What To Verify Before Handoff","text":"<p>Run the API locally:</p> <pre><code>npm run dev\n</code></pre> <p>Then verify:</p> <pre><code>curl http://localhost:3000/api/health\ncurl http://localhost:3000/api/sources\n</code></pre> <p>For Copernicus compatibility work, run:</p> <pre><code>python tests_external/compare_scene_search.py\npython tests_external/compare_scene_download.py\n</code></pre> <p>The scene search comparison should show no missing items and the same ordering.</p>"},{"location":"qa/#troubleshooting","title":"Troubleshooting","text":""},{"location":"qa/#copernicus_auth_missing","title":"<code>COPERNICUS_AUTH_MISSING</code>","text":"<p>The Sentinel Hub Catalog or Process mode needs credentials.</p> <p>Set either:</p> <pre><code>COPERNICUS_ACCESS_TOKEN=...\n</code></pre> <p>or:</p> <pre><code>COPERNICUS_CLIENT_ID=...\nCOPERNICUS_CLIENT_SECRET=...\n</code></pre>"},{"location":"qa/#copernicus_auth_error","title":"<code>COPERNICUS_AUTH_ERROR</code>","text":"<p>The service tried to acquire a token but CDSE rejected the request.</p> <p>Check:</p> <ul> <li>client ID</li> <li>client secret</li> <li>token URL</li> <li>account permissions</li> </ul>"},{"location":"qa/#external_api_error","title":"<code>EXTERNAL_API_ERROR</code>","text":"<p>The external provider returned a non-success response.</p> <p>The error message includes provider status and a short response detail. It should not include secrets.</p>"},{"location":"qa/#external_api_timeout","title":"<code>EXTERNAL_API_TIMEOUT</code>","text":"<p>The provider request exceeded <code>REQUEST_TIMEOUT_MS</code>.</p> <p>Increase the timeout for large Process API calls:</p> <pre><code>REQUEST_TIMEOUT_MS=180000\n</code></pre>"},{"location":"qa/#route_not_found-for-apiingestionrun","title":"<code>ROUTE_NOT_FOUND</code> for <code>/api/ingestion/run</code>","text":"<p>The route only supports <code>POST</code>.</p> <p>Use:</p> <pre><code>curl -X POST http://localhost:3000/api/ingestion/run\n</code></pre>"},{"location":"qa/#scene-search-results-differ","title":"Scene Search Results Differ","text":"<p>Check that both direct and ingestion paths use the same:</p> <ul> <li>bbox</li> <li>date range</li> <li>maximum cloud percentage</li> <li>maximum images</li> <li>credentials</li> <li>CDSE catalog endpoint</li> </ul> <p>The compatibility wrapper filters cloud coverage and sorts by datetime descending to match the original Python implementation.</p>"},{"location":"qa/#download-comparison-differs","title":"Download Comparison Differs","text":"<p>Check that both paths use the same scene datetime and bbox. The Process API request depends on the time window and calculated output dimensions.</p> <p>Also confirm the caller writes the ingestion response bytes exactly after base64 decoding.</p>"},{"location":"workflow/","title":"Workflow","text":"<p>The current alpha workflow is request-response only.</p>"},{"location":"workflow/#workflow-boundary","title":"Workflow Boundary","text":"<p>The production TERRA concept includes orchestrator-driven DAGs, storage operations, and metadata reports describing changed database entries and file locations. Those steps are not owned by this alpha service.</p> <p>In this version:</p> <ul> <li>the caller sends a request</li> <li>the service calls the external provider</li> <li>the service wraps the response</li> <li>the service returns the wrapped response</li> <li>the caller or orchestrator owns persistence and next pipeline steps</li> </ul>"},{"location":"workflow/#standard-runtime-workflow","title":"Standard Runtime Workflow","text":"<p>The standard runtime workflow is:</p> <ol> <li>The caller builds an ingestion payload and sends <code>POST /api/ingestion/run</code>.</li> <li>The service normalizes and validates the request.</li> <li>If the requested source is not registered, the service returns <code>UNKNOWN_SOURCE</code>.</li> <li>If the source exists, the service builds the external provider request and calls the provider.</li> <li>If the provider call fails, the service returns <code>EXTERNAL_API_ERROR</code> or <code>EXTERNAL_API_TIMEOUT</code>.</li> <li>If the provider call succeeds, the service selects a wrapper using <code>responseProfile</code>.</li> <li>The wrapper transforms the provider response.</li> <li>The service returns the stateless result to the caller.</li> </ol>"}]}